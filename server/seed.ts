// Seed disabled - PostgreSQL database is managed separately
export function seedDatabase() {
  // No-op: PostgreSQL database does not need seeding on startup
  // Use the admin panel to create initial data if needed
}
