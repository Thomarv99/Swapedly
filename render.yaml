services:
  - type: web
    name: swapedly
    runtime: node
    buildCommand: npm ci && npm run build
    startCommand: npm start
    plan: starter
    envVars:
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
      - key: RENDER
        value: true
      - key: PADDLE_API_KEY
        sync: false
      - key: PADDLE_CLIENT_TOKEN
        sync: false
      - key: PADDLE_WEBHOOK_SECRET
        sync: false
      - key: PADDLE_PRICE_PLUS_MONTHLY
        sync: false
      - key: PADDLE_PRICE_CREDITS_10
        sync: false
      - key: PADDLE_PRICE_CREDITS_25
        sync: false
      - key: PADDLE_PRICE_CREDITS_50
        sync: false
    disk:
      name: swapedly-data
      mountPath: /data
      sizeGB: 1
